name: 🚀 Minerador Automático Edge Pro

on:
  schedule:
    - cron: '0 11,21 * * *' 
  workflow_dispatch: 

jobs:
  executar_minerador:
    runs-on: ubuntu-latest
    
    steps:
      - name: 1. Baixando os arquivos
        uses: actions/checkout@v3

      - name: 2. Configurando Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 3. Instalando bibliotecas
        run: |
          pip install requests psycopg2-binary pytz

      - name: 4. Executando o Robô
        # AQUI ESTAVA O ERRO: Faltava passar as chaves do Telegram!
        env:
          DB_URL: ${{ postgresql://postgres.vbxmtclyraxmhvfcnfee:MudarAgora2026Paraiba@aws-1-sa-east-1.pooler.supabase.com:6543/postgres }}
          TELEGRAM_TOKEN: ${{ 8344469814:AAHvhEKG9_6qfaPc-1gdjYdcBGn6qdcfFeg }}   # <--- LINHA NOVA
          TELEGRAM_CHAT_ID: ${{ 1343274781 }} # <--- LINHA NOVA
        run: python worker_saas.py